# This file is auto-generated by scripts/generate-e2e-artifacts.mjs
# Do not edit manually. Run `npm run e2e:generate` to regenerate.
name: verify
on:
  push:
    branches: ['**']
    tags: ['**']
  pull_request:
    branches: ['**']

permissions:
  actions: read
  contents: read

jobs:
  verify:
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v4

      - name: Load pr-any filter
        id: load-pr-any
        run: |
          { echo 'yaml<<EOF'; cat e2e/generated/on-filter/pr-any.yml; echo 'EOF'; } >> "$GITHUB_OUTPUT"
      - name: Run on-filter pr-any
        uses: ./
        id: on-filter-pr-any
        with:
          on: ${{ steps.load-pr-any.outputs.yaml }}

      - name: Load pr-main filter
        id: load-pr-main
        run: |
          { echo 'yaml<<EOF'; cat e2e/generated/on-filter/pr-main.yml; echo 'EOF'; } >> "$GITHUB_OUTPUT"
      - name: Run on-filter pr-main
        uses: ./
        id: on-filter-pr-main
        with:
          on: ${{ steps.load-pr-main.outputs.yaml }}

      - name: Load pr-types-opened filter
        id: load-pr-types-opened
        run: |
          { echo 'yaml<<EOF'; cat e2e/generated/on-filter/pr-types-opened.yml; echo 'EOF'; } >> "$GITHUB_OUTPUT"
      - name: Run on-filter pr-types-opened
        uses: ./
        id: on-filter-pr-types-opened
        with:
          on: ${{ steps.load-pr-types-opened.outputs.yaml }}

      - name: Load pr-types-sync filter
        id: load-pr-types-sync
        run: |
          { echo 'yaml<<EOF'; cat e2e/generated/on-filter/pr-types-sync.yml; echo 'EOF'; } >> "$GITHUB_OUTPUT"
      - name: Run on-filter pr-types-sync
        uses: ./
        id: on-filter-pr-types-sync
        with:
          on: ${{ steps.load-pr-types-sync.outputs.yaml }}

      - name: Load push-any-branch filter
        id: load-push-any-branch
        run: |
          { echo 'yaml<<EOF'; cat e2e/generated/on-filter/push-any-branch.yml; echo 'EOF'; } >> "$GITHUB_OUTPUT"
      - name: Run on-filter push-any-branch
        uses: ./
        id: on-filter-push-any-branch
        with:
          on: ${{ steps.load-push-any-branch.outputs.yaml }}

      - name: Load push-ignore-main filter
        id: load-push-ignore-main
        run: |
          { echo 'yaml<<EOF'; cat e2e/generated/on-filter/push-ignore-main.yml; echo 'EOF'; } >> "$GITHUB_OUTPUT"
      - name: Run on-filter push-ignore-main
        uses: ./
        id: on-filter-push-ignore-main
        with:
          on: ${{ steps.load-push-ignore-main.outputs.yaml }}

      - name: Load push-main filter
        id: load-push-main
        run: |
          { echo 'yaml<<EOF'; cat e2e/generated/on-filter/push-main.yml; echo 'EOF'; } >> "$GITHUB_OUTPUT"
      - name: Run on-filter push-main
        uses: ./
        id: on-filter-push-main
        with:
          on: ${{ steps.load-push-main.outputs.yaml }}

      - name: Load push-paths-ignore-docs filter
        id: load-push-paths-ignore-docs
        run: |
          { echo 'yaml<<EOF'; cat e2e/generated/on-filter/push-paths-ignore-docs.yml; echo 'EOF'; } >> "$GITHUB_OUTPUT"
      - name: Run on-filter push-paths-ignore-docs
        uses: ./
        id: on-filter-push-paths-ignore-docs
        with:
          on: ${{ steps.load-push-paths-ignore-docs.outputs.yaml }}

      - name: Load push-paths-src filter
        id: load-push-paths-src
        run: |
          { echo 'yaml<<EOF'; cat e2e/generated/on-filter/push-paths-src.yml; echo 'EOF'; } >> "$GITHUB_OUTPUT"
      - name: Run on-filter push-paths-src
        uses: ./
        id: on-filter-push-paths-src
        with:
          on: ${{ steps.load-push-paths-src.outputs.yaml }}

      - name: Load push-tag-ignore-rc filter
        id: load-push-tag-ignore-rc
        run: |
          { echo 'yaml<<EOF'; cat e2e/generated/on-filter/push-tag-ignore-rc.yml; echo 'EOF'; } >> "$GITHUB_OUTPUT"
      - name: Run on-filter push-tag-ignore-rc
        uses: ./
        id: on-filter-push-tag-ignore-rc
        with:
          on: ${{ steps.load-push-tag-ignore-rc.outputs.yaml }}

      - name: Load push-tag-v filter
        id: load-push-tag-v
        run: |
          { echo 'yaml<<EOF'; cat e2e/generated/on-filter/push-tag-v.yml; echo 'EOF'; } >> "$GITHUB_OUTPUT"
      - name: Run on-filter push-tag-v
        uses: ./
        id: on-filter-push-tag-v
        with:
          on: ${{ steps.load-push-tag-v.outputs.yaml }}

      - name: Wait for oracle workflows to be enqueued
        run: sleep 60

      - name: Compare on-filter results with oracle workflows
        uses: actions/github-script@v7
        with:
          script: |
            const { data } = await github.rest.actions.listWorkflowRunsForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              head_sha: context.sha,
              per_page: 100,
            });
            const oracleRuns = new Set(
              data.workflow_runs
                .filter(r => r.name.startsWith('oracle-'))
                .map(r => r.name.replace('oracle-', ''))
            );
            const results = {
              'pr-any': '${{ steps.on-filter-pr-any.outputs.result }}',
              'pr-main': '${{ steps.on-filter-pr-main.outputs.result }}',
              'pr-types-opened': '${{ steps.on-filter-pr-types-opened.outputs.result }}',
              'pr-types-sync': '${{ steps.on-filter-pr-types-sync.outputs.result }}',
              'push-any-branch': '${{ steps.on-filter-push-any-branch.outputs.result }}',
              'push-ignore-main': '${{ steps.on-filter-push-ignore-main.outputs.result }}',
              'push-main': '${{ steps.on-filter-push-main.outputs.result }}',
              'push-paths-ignore-docs': '${{ steps.on-filter-push-paths-ignore-docs.outputs.result }}',
              'push-paths-src': '${{ steps.on-filter-push-paths-src.outputs.result }}',
              'push-tag-ignore-rc': '${{ steps.on-filter-push-tag-ignore-rc.outputs.result }}',
              'push-tag-v': '${{ steps.on-filter-push-tag-v.outputs.result }}',
            };
            let failures = 0;
            for (const [name, result] of Object.entries(results)) {
              const oracleRan = oracleRuns.has(name);
              const expected = result === 'true';
              if (expected !== oracleRan) {
                core.error(`MISMATCH: ${name} on-filter=${result} oracle=${oracleRan}`);
                failures++;
              } else {
                core.info(`OK: ${name} on-filter=${result} oracle=${oracleRan}`);
              }
            }
            if (failures > 0) core.setFailed(`${failures} mismatch(es)`);

