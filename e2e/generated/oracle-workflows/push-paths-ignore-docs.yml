name: oracle-push-paths-ignore-docs
on:
  push:
    paths-ignore:
      - docs/**
      - '*.md'
jobs:
  oracle:
    runs-on: ubuntu-slim
    steps:
      - run: echo oracle workflow trigger definition
